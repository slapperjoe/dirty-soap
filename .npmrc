# APInox npm config — applies to every npm install in the repo tree.
#
# Why: a stalled TLS response from the registry hangs `npm install` forever
# (npm has no timeout for in-flight responses). Keep install steps quiet
# about non-essential network chatter so fewer round-trips can stall.
audit=false
fund=false
update-notifier=false
